# Data-Augmentation-Matlab
A scripts for data augmentation using image processing operations such as flipping, rotation, etc. It enables users cropping the image before performing augmentation operations. 
